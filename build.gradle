apply plugin: 'java'
apply plugin: 'application'

allprojects {
  repositories {
    mavenCentral()
  }
}

project(':tester') {
  apply plugin: 'application'

}

project(':webapp') {
  apply plugin: 'war'

  dependencies {
    providedCompile 'javax.servlet:javax.servlet-api:3.0.1'
  }
}
